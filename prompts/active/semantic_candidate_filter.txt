You are given ordered images ending with one candidate crop from the query image.
Decide whether the final candidate crop should remain a valid Stage-3 candidate.

Return only the following XML-like field:
<keep>yes or no</keep>

Rules:
- The final image is always the candidate crop.
- If earlier images are present, they provide context only. Do not classify the crop into a category here.
- Return `yes` for a real object or document crop even when the exact subtype is uncertain.
- Return `no` only if the crop is clearly background, wall, floor, furniture, a non-target body-part region, empty clutter, or otherwise not a coherent private-object/document candidate.
- Do not reject a crop only because two nearby private-object subtypes are hard to distinguish.
- Do not output any extra text.
