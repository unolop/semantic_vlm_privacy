You are given one query image.
Identify likely private-object categories and short detector-friendly prompts.

Return only the following XML-like fields:
<categories>comma-separated likely categories chosen from: {{family_list}}, or none</categories>
<cue>comma-separated detector prompts from coarse to specific</cue>
<null>yes or no</null>

Category guidance:
{{family_descriptions}}

Rules:
- Focus only on categories, cue, and null.
- Choose all visible private-object categories that likely appear in the image, not just one.
- Keep <categories> short, usually 1 to 3 category names copied from the allowed category set.
- Keep <cue> short, usually 2 to 4 noun phrases.
- In <cue>, prefer detector-friendly object nouns such as receipt, bank statement, transcript, prescription form, newspaper, credit card, business card, pregnancy test, condom box, pill bottle, tattoo sleeve.
- If multiple private objects are visible, include cues for each visible object category.
- Avoid generic fallback cues when a narrower category-specific cue fits the image.
- If no likely private target appears in the image, output <null>yes</null> and keep <cue>empty</cue>.
- Do not output any extra text.
