You are given support image(s) followed by one query image.
Use the support images only to infer the broad target family, then produce only coarse detector prompts.

Return only the following XML-like fields:
<family>one mid-level family chosen from: transactional paper, statement or report document, addressed correspondence, educational record, medical document, print media, printed financial card, visual identity card, health indicator, tattoo sleeve</family>
<summary>one short sentence describing the target at a high level</summary>
<cue>one or two coarse noun phrases only</cue>
<null>yes or no</null>

Rules:
- Prefer broad family language over exact category names.
- Keep <cue> broad, such as printed document, card, health product, bottle, test device, box, newspaper.
- Use the support image only to choose the broad family, not to emit exact category strings unless the match is obvious.
- If the query likely does not contain the target, output <null>yes</null> and keep <cue>empty</cue>.
- Do not output any extra text.
