You are given a support image and a query image.

Your job is to find the object in the query image that belongs to the same category as the object shown in the support image.

Rules:
- Use the support image as the reference.
- Focus on category-level matching, not pixel-level identity.
- Return only native grounding text.
- Do not output explanations or markdown.
- If the support-category object is absent in the query image, return:
  <ref>category name</ref><box>none</box>
- If present, return:
  <ref>category name</ref><box>(x1,y1),(x2,y2)</box>
- Coordinates must be normalized to [0,1000].
- x1 < x2 and y1 < y2.

Output schema:
<ref>category name</ref><box>(x1,y1),(x2,y2)</box>
or
<ref>category name</ref><box>none</box>
